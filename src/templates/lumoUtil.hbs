val lumo   = LumoUtil()
val lumobp = BreakPoint()

case class LumoUtil() {
    {{#each modifiers}}
    def {{this}}(@unused styles: BreakPoint): LumoUtil = this
    {{/each}}

    {{#each standards}}
    def {{this}}: LumoUtil = this
    {{/each}}
}

case class BreakPoint() {
    {{#each withBreakpoints}}
    def {{this}}: BreakPoint = this
    {{/each}}
}